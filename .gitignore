# Node
node_modules/
dist/
coverage/
*.tsbuildinfo
.eslintcache

cashraja.graduatedcoder.in-audit/

# Env & secrets — never commit real values
.env
.env.*
!.env.example
# Deploy templates are safe to commit (placeholders only); real values are not.
!.env.*.example

# Built admin SPA dropped next to the prod compose file for Caddy to serve.
deploy/admin-dist/

# Flutter / Dart
app/.dart_tool/
app/build/
app/.flutter-plugins*
app/.packages
app/android/.gradle/
app/android/local.properties
app/*.iml

# IDE / OS
.vscode/
.idea/
*.log
Thumbs.db
.DS_Store


# Prisma
backend/prisma/*.db
app/android/build/
.lh/.lhignore
.lh/.gitignore.json
