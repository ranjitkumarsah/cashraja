# Cash Raja — https://cashraja.graduatedcoder.in
# Public marketing pages are open to ALL crawlers, including AI / answer engines
# (GPTBot, PerplexityBot, ClaudeBot, Google-Extended, etc. — they inherit this
# group). The signed-in app routes are kept out of the index via a noindex meta
# tag, not robots.txt, so they can still be rendered without being indexed.
# Only the admin console and the JSON API are disallowed here.

User-agent: *
Allow: /
Disallow: /admin/
Disallow: /api/

Sitemap: https://cashraja.graduatedcoder.in/sitemap.xml
